import AuthLayout from '@/layouts/AuthLayout';

type Props = {};

const About = (props: Props) => {
  return (
    <AuthLayout>
      <div>About page</div>
    </AuthLayout>
  );
};

export default About;
